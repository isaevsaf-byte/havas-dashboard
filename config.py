"""Настройки дашборда.

Дашборд живёт отдельно от сервиса подсчёта: он только читает данные из
Supabase и ничего не знает про камеру, линию подсчёта и модели. Поэтому здесь
пять значений, а не весь конфиг пайплайна.

Ключи задаются в Streamlit Cloud: App settings -> Secrets. В коде их нет и
быть не должно — именно поэтому этот репозиторий может быть публичным, пока
основной остаётся закрытым.
"""

import os

SUPABASE_URL = os.getenv("SUPABASE_URL", "")
SUPABASE_KEY = os.getenv("SUPABASE_KEY", "")

STORE_NAME = os.getenv("STORE_NAME", "havas_tashkent")

# Проходы, которые система не смогла опознать, пишутся с этим префиксом.
# Они считаются как посещаемость, но исключаются из метрик уникальности:
# иначе плохой кадр выглядит как новый посетитель.
UNKNOWN_VISITOR_PREFIX = "unknown-"

DASHBOARD_REFRESH_SEC = 30

# Видит ли камера, как люди уходят из магазина. На havas_tashkent — нет:
# вход и выход разные двери. События OUT там означают возврат через входную
# дверь, а не уход, и подписывать их «Вышел» значит вводить в заблуждение.
EXIT_IN_FRAME = os.getenv("EXIT_IN_FRAME", "") == "1"
